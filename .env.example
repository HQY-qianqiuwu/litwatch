# Local settings are introduced with their corresponding Phase 2+ features.
# Copy to .env when those features exist. Never commit .env or real credentials.
LITWATCH_DATABASE_PATH=./data/litwatch.db
LITWATCH_OPENALEX_EMAIL=
LITWATCH_CROSSREF_EMAIL=

# BYOK LLM keys will be supplied per analysis request, not saved here.
